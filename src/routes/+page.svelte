<script>
	import welcome from '$lib/images/svelte-welcome.webp';
	import welcome_fallback from '$lib/images/svelte-welcome.png';
</script>

<svelte:head>
	<title>aparimeya.ai</title>
	<meta name="description" content="Ap" />
</svelte:head>

<section>
	<h1>
		Under Construction 
	</h1>
	🤫
</section>

<style>
	section {
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		height: 350px;
		width: 300px;
		background-color: rgba(20, 20, 20,.2);
		color: white;
		border-radius: 10px;
		box-shadow:
    inset 0 0 5px  rgba(255, 255, 255,.95),
	inset 20px 0 80px rgba(255, 0, 255,.5),
    inset -20px 0 80px rgba(0, 255, 255,.5),
    inset 20px 0 300px rgba(255, 0, 255,.5),
    inset -20px 0 300px rgba(0, 255, 255,.5),
    0 0 50px rgba(255, 255, 255,.5),
    -10px 0 80px rgba(255, 0, 255,.5),
    10px 0 80px rgba(0, 255, 255,.5);

	}

	h1 {

		background-color: red;
  
  /* Create the gradient. */
  background-image: linear-gradient(45deg, #8fc3a4, #bf91ff);
  
  /* Set the background size and repeat properties. */
  background-size: 100%;
  background-repeat: repeat;

  /* Use the text as a mask for the background. */
  /* This will show the gradient as a text color rather than element bg. */
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent; 
  -moz-background-clip: text;
  -moz-text-fill-color: transparent;
}
	
</style>
